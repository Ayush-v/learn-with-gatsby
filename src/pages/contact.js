import * as React from "react";
import Layout from "../components/layout";
// import { Link } from "gatsby";

const ContactPage = () => {
  return (
    <Layout pageTitle="Contact Us" pageHeading="Contact">
      Learning how to use childrens in react.
    </Layout>
  );
};

export default ContactPage;
